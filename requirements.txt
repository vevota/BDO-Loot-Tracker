# BDO Loot Tracker – Python dependencies (Linux/Wayland)
# Install with:  pip install -r requirements.txt
#
# System dependencies (install via your package manager):
#   pacman -S tesseract grim slurp
#
# Tesseract is required for OCR.

Pillow>=10.0.0       # image processing
pystray>=0.19.5      # system tray icon
requests>=2.31.0     # HTTP uploads to Supabase
pytesseract>=0.3.10  # Tesseract OCR bindings
python-dotenv>=1.0.1 # load local .env configuration
opencv-python-headless>=4.8.0  # image preprocessing
PySide6>=6.7.0       # native desktop window host for local UI
